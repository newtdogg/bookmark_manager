<h1>
  Links
</h1>

<ul id='links'> <!-- ul = unordered list, ol = ordered list -->
  <% @links.each do |link| %>
    <li> <!-- li = list, this is a child tag to the parent tag -->
      ID: <%= link.id %>
      Title: <%= link.title %>
      URL: <%= link.url %>
    </li>
  <% end %>
</ul>
